#include <iostream>
#include <string>
using namespace std;

int main() {
    double sum = 0;
    double n = 0;
    double x = 0;

    x = 1;
    sum = 0;
    if (x <= n) {
    sum = sum + x;
    x = x + 1;
    } else {
    cout << sum << endl;
    return 0;
    }
    return 0;
}
