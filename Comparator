#include <iostream>
#include <string>
using namespace std;

int main() {
    double x = 0;
    double y = 0;

    cin >> x;
    cin >> y;
    if (x > y) {
    cout << x << endl;
    return 0;
    } else {
    cout << y << endl;
    }
    return 0;
}
