#include <iostream>
#include <string>
using namespace std;

int main() {
    double x = 0;
    double sum = 0;
    double n = 0;

    x = 1;
    sum = 0;
    cin >> n;
    if (x <= n) {
    sum = sum + x;
    x = x + 1;
    } else {
    cout << sum << endl;
    return 0;
    }
    return 0;
}
